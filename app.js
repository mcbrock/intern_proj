const express = require('express');
const app = express();
const port = 22; //we probs need to specify a port, because so many people are using it 

let videos = [];

app.post("/NemoText",(req,res) => {

});

app.listen()

//iframe in HTML?